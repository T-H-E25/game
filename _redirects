# Netlify redirects file for SPA routing
# This ensures all routes serve the React app

/*    /index.html   200

# API routes should not be redirected (if you add any later)
/api/*  /api/:splat  200

# Health check endpoint
/.well-known/*  /.well-known/:splat  200